/**
 * /dev/docs — full-page documentation browser.
 * Displays the same API / Commands / Widgets index as the ⌘K pop-up.
 */
import { DocsPanelFull } from '@/components/terminal/DocsPanelFull'

export function DevDocs() {
  return <DocsPanelFull />
}
