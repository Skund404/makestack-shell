"""Global constants for the Makestack Shell.

SHELL_VERSION is the single source of truth for the running shell version.
Never hard-code the version string anywhere else — always import from here.
"""

SHELL_VERSION = "0.1.0"
